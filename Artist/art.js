const newArray = [];
var imgSrc;

function popImageA() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-1').attr("src",imgSrc);
}

function popImageB() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-2').attr("src",imgSrc);
}

function popImageC() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-3').attr("src",imgSrc);
}

function popImageD() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-4').attr("src",imgSrc);
}

function popImageE() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-5').attr("src",imgSrc);
}
function popImageF() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-6').attr("src",imgSrc);
}
function popImageG() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-7').attr("src",imgSrc);
}
function popImageH() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-8').attr("src",imgSrc);
}

function popImageI() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-9').attr("src",imgSrc);
}

function popImageJ() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-10').attr("src",imgSrc);
}

function popImageK() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-11').attr("src",imgSrc);
}

function popImageL() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-12').attr("src",imgSrc);
}

function popImageM() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-13').attr("src",imgSrc);
}

function popImageN() {
	for (i=0;i<imageLocation.length;i++) {
		let test = imageLocation[i].loc;
		newArray.push(test);
	}
	var random = newArray[Math.floor(Math.random()*newArray.length)]
	imgSrc = random;
	//document.getElementsByClassName('img-only').setAttribute('src', imgSrc);
	$('#img-14').attr("src",imgSrc);
}